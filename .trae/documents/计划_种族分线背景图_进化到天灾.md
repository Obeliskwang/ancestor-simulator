## 总结

为不同种族分线配置独立背景图，并在“自身进化 → 种族冲突 → 随机奇遇 → 天灾考验”全过程显示（带暗色遮罩凸显文字）；结局页与隐藏结局页不显示，保持现有背景。

背景资源（已在仓库内）：
- 陆地线（羸弱原兽 / `smilodon`）：`images/bg/land_bg.png`
- 海洋线（孱弱古鱼 / `dunkleosteus`）：`images/bg/sea_bg.png`
- 天空线（无翼古雏 / `confuciusornis`）：`images/bg/sky_bg.png`

## 现状分析（基于代码）

- 背景绘制集中在 [ui.js](file:///e:/Obeliskw/ancestor-simulator/js/ui.js)：
  - `UI.init()` 目前只加载了首页背景 `images/bg/bg.jpg`。
  - `UI.drawBackground()` 绘制通用渐变底色（低画质下不绘制额外噪点）。
  - 进化/冲突/奇遇界面均调用 `UI.drawBackground()`。
  - 天灾界面当前在 [game.js](file:///e:/Obeliskw/ancestor-simulator/js/game.js) 内用红色渐变直接 `fillRect`，不走 `UI.drawBackground()`。
  - 结局页调用 `UI.drawBackground()`（且包含隐藏结局模式渲染），需要保持不变。

## 拟修改内容（具体到文件）

### 1) 加载三套分线背景图 + 提供统一绘制函数

**文件**：[ui.js](file:///e:/Obeliskw/ancestor-simulator/js/ui.js)

- 在 `UI.init()` 中新增加载：
  - `this.images.landBg.src = './images/bg/land_bg.png'`
  - `this.images.seaBg.src = './images/bg/sea_bg.png'`
  - `this.images.skyBg.src = './images/bg/sky_bg.png'`
- 新增函数（UI 内部方法）：
  - `drawRouteBackground(raceId)`：
    - 根据 `raceId` 映射到对应 Image（`smilodon`→land、`dunkleosteus`→sea、`confuciusornis`→sky）
    - 采用“cover”方式铺满画布（与 `drawStartScreen` 背景绘制方式一致）
    - 绘制暗色遮罩（例如 `ctx.fillStyle = 'rgba(0,0,0,0.55)'` 或使用现有 `UI.colors.overlay` 并适当调低透明度），保证文字对比度
    - 若图片未加载完成（`!img.complete` 或尺寸为 0），回退到 `UI.drawBackground()`

### 2) 进化/冲突/奇遇/天灾使用分线背景；结局页不改

**文件**：[game.js](file:///e:/Obeliskw/ancestor-simulator/js/game.js)

- `screens.evolution.render`：
  - 将 `UI.drawBackground()` 替换为 `UI.drawRouteBackground(Game.state.selectedRace?.id)`
- `screens.conflict.render`：
  - 同上替换为分线背景
- `screens.encounter.render`：
  - 同上替换为分线背景
- `screens.disaster.render`：
  - 移除当前红色渐变底（或保留为额外轻量叠加层），改为：
    - 先 `UI.drawRouteBackground(Game.state.selectedRace?.id)` 绘制分线背景
    - （可选）再叠加一层较低透明度的红色渐变，用于维持“天灾氛围”，但不影响文字可读性
- `screens.ending.render`：
  - 保持现有 `UI.drawBackground()` 不变（包含隐藏结局展示页），确保“结局页和隐藏结局页不显示分线背景”

## 决策与假设

- 分线背景仅从“进化”到“天灾”四个阶段生效；开始页/抽卡页保持现有表现。
- 遮罩以“暗色凸显文字”为第一优先，强度默认统一；如你后续希望不同分线不同遮罩强度再单独调整。

## 验证步骤

1) 分别选择三种族进入进化阶段：
   - `smilodon` 显示 `land_bg.png`
   - `dunkleosteus` 显示 `sea_bg.png`
   - `confuciusornis` 显示 `sky_bg.png`
2) 继续推进到冲突/奇遇/天灾：
   - 背景图贯穿不变，且文字在暗色遮罩下清晰可读
3) 进入结局页/隐藏结局页：
   - 仍为原结局背景（不显示分线背景）
4) 控制台无报错；图片未加载完成时能自动回退到通用背景（不黑屏）。

