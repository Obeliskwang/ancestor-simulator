## 总结

1) 将“种族冲突 / 随机奇遇 / 天灾考验”的选项按钮交互与样式统一为“自身进化”的选项框：点击后仅变深（pressed/selected），不再有额外闪烁/特效差异。  
2) 将“随机奇遇”界面里的剧情描述与选项文字改为按宽度自动换行分行展示，避免一行挤满。

## 现状分析（基于代码）

- 进化选项样式基准在 [game.js](file:///e:/Obeliskw/ancestor-simulator/js/game.js#L240-L325)：
  - 选项按钮：统一底色 `UI.colors.primaryDark`，点击/选中仅 `UI.darkenColor(..., 18)`；
  - 不做缩放/闪烁，仅 stroke、fill、文字。
- 冲突选项当前已基本符合（同样使用 primaryDark + darken），位于 [game.js](file:///e:/Obeliskw/ancestor-simulator/js/game.js#L479-L560)。
- 天灾选项当前使用红色系 `baseFillA/baseFillB`（与进化不一致），位于 [game.js](file:///e:/Obeliskw/ancestor-simulator/js/game.js#L912-L1003)。
- 奇遇选项当前 hidden/normal 两个按钮配色不一致（hiddenOption 有特殊底色/描边），且剧情描述与按钮文字均为单行 `Utils.drawText`，位于 [game.js](file:///e:/Obeliskw/ancestor-simulator/js/game.js#L700-L789)。

## 改动方案（决策已锁定）

- 去闪范围：只改冲突/奇遇/天灾的“选项框”交互与样式，顶部进度条与底部阶段条不动。  
- 奇遇分行：采用“渲染时按宽度自动换行”，不改 data.js 文案内容。

## 拟修改内容（具体到文件）

### 1) 统一选项框样式与点击反馈（仿照进化）

**文件**： [game.js](file:///e:/Obeliskw/ancestor-simulator/js/game.js)

改动点：
- `screens.conflict.render`：
  - 复核并对齐进化样式：`baseFill = UI.colors.primaryDark`、`stroke = UI.colors.primary`、`text color = UI.colors.textLight`、`size = 13`；
  - 点击后确保“只有变深”：如有必要，在第一次点击后将 `s.optionAlpha = 1`（避免点击时仍处于淡入造成观感闪动）。
- `screens.disaster.render`：
  - 将 `baseFillA/baseFillB` 改为进化同款：`baseFill = UI.colors.primaryDark`；
  - 描边统一用 `UI.colors.primary`；
  - 文本颜色统一 `UI.colors.textLight`；
  - 点击后同上：必要时在 `onClick` 首次选择时将 `s.optionAlpha = 1`。
- `screens.encounter.render`：
  - 将 hidden/normal 两个选项按钮的底色/描边统一为进化同款（不再使用 hidden 的特殊配色）；
  - 点击后同上：必要时在 `onClick` 首次选择时将 `s.optionAlpha = 1`。

### 2) 奇遇剧情与选项文字自动换行

**文件**： [game.js](file:///e:/Obeliskw/ancestor-simulator/js/game.js)

改动点（仅 encounter 屏）：
- 剧情框（`encounter.description`）：
  - 在奇遇剧情面板宽度内（`w * 0.8` 减去左右 padding）按 `ctx.measureText` 自动折行；
  - 控制最大行数（建议 3 行），超出截断；
  - 按行绘制（固定 `lineHeight`），整体居中或顶部对齐，避免与标题重叠。
- 选项按钮文字（`encounter.hiddenOption.text` / `encounter.normalOption.text`）：
  - 在按钮宽度（`optW`）减 padding 内自动折行；
  - 控制最大行数（建议 2 行），按按钮中心垂直居中绘制多行文本。

实现方式：
- 直接在 encounter.render 内复用“结局描述自动换行”的写法（逐字符累加 + measureText），避免引入新依赖与大范围重构。

## 验证步骤（跑通即可）

1) 进入“冲突/奇遇/天灾”任一界面，快速点击选项：
   - 选项框立即变深并保持，不出现额外闪动/缩放/柔光。
2) 进入“随机奇遇”：
   - 剧情描述在面板内自动分行；
   - 两个选项按钮文字在按钮内自动分行，不再一行挤满或溢出。
3) 全程控制台无报错（重点关注 `ctx.measureText` 用前是否 `ctx.font` 已设置）。

