# 计划：补齐“种族冲突”阶段结算展示（按 PRD 5.3）

## 摘要
在“种族冲突（领地争夺战）”6 题结束后，补齐与“自身进化”一致的结算展示界面：显示档位评价文案 + 进度加成数值，并在短暂停留后自动进入“随机奇遇”阶段。

## 现状分析（基于现有实现）
- 当前 `conflict` 阶段已经完成：
  - 6 题流程、选项左右随机、进取计分（`aggressive`）、档位判定（≥4/2~3/≤1）。见：[game.js](file:///e:/Obeliskw/ancestor-simulator/js/game.js#L435-L614)
  - `showResult/resultAlpha/resultTimer` 也已具备，并在 2 秒后按档位给进度加成并跳到 `encounter`。见：[game.js](file:///e:/Obeliskw/ancestor-simulator/js/game.js#L477-L491)
- 但 `conflict.render()` 目前只渲染题面/选项，没有像 `evolution.render()` 那样在 `showResult=true` 时切到“结算面板”显示文案与加成，因此玩家会感觉“没有结算”。见：[game.js](file:///e:/Obeliskw/ancestor-simulator/js/game.js#L495-L582)

## 需求对齐（PRD 5.3）
### 数值规则（PRD 5.3.2）
- 进取 +1 分，保守 0 分
- 总分 ≥ 4：优秀（+35%）
- 总分 2~3：普通（+15%）
- 总分 ≤ 1：劣质（+0%）

### 结算文案（PRD 5.3.4）
- 优秀：族群勇猛善战，步步向外扩张，在领地争夺中所向披靡，整体实力大幅提升。
- 普通：行事犹豫不决，攻守摇摆不定，族群发展平稳，却难以抢占更多生存优势。
- 劣质：一味避战退让，接连错失发展良机，族群活动范围不断受限，生存压力加剧。

## 改动方案（仿照“自身进化”结算）

### 1) 在 conflict.render 中加入 showResult 分支
**文件**：[game.js](file:///e:/Obeliskw/ancestor-simulator/js/game.js)
- 参考 `evolution.render()` 的结构（`if (!s.showResult) { ...题面... } else { ...结算面板... }`），为 `conflict.render()` 增加同样的 `else` 分支。
- 结算面板绘制内容：
  - 标题：`领地战评价`（或 `冲突结算`，固定其一）
  - 正文：使用 PRD 5.3.4 对应档位文案（必要时拆成 1~2 行绘制）
  - 进度加成：`进度 +35% / +15% / +0%`（文本显示，数值来源于现有 grade）
  - 面板描边颜色：与进化一致（优秀金 / 普通绿 / 劣质红）
- 与进化保持一致：`ctx.globalAlpha = s.resultAlpha` 控制淡入。

### 2) update 逻辑保持现状（只补齐展示）
**文件**：[game.js](file:///e:/Obeliskw/ancestor-simulator/js/game.js)
- 不改变现有计分与跳转时序：
  - `showResult` 后 `resultAlpha` 渐入
  - 2 秒后结算加成写入 `Game.state.progress` 并 `switchScreen('encounter')`
- 如渲染需要，在 `conflict.init()` 的 `screenState` 中补充 `resultTextLines`（可选；不强制），但优先直接在 render 内按 grade 选择文案，避免增加状态字段。

## 验证步骤
1) 从进化阶段正常进入冲突阶段，完成 6 题。
2) 第 6 题选择后：
  - 出现“领地战评价/冲突结算”面板
  - 显示对应档位文案（优秀/普通/劣质之一）
  - 显示 `进度 +35%/+15%/+0%`
3) 停留约 2 秒后自动进入奇遇阶段，且进度值正确累加。
4) 三档覆盖测试：
  - 进取次数 ≥4、2~3、≤1 分别触发三种文案与加成。

